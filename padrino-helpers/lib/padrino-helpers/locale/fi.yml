fi:
  number:
    # Used in number_with_delimiter().
    # These are also the defaults for 'currency', 'percentage', 'precision', and 'human'.
    format:
      # Sets the separator between the units, for more precision (e.g. 1.0 / 2.0 == 0.5).
      separator: ","
      # Delimits thousands (e.g. 1,000,000 is a million) (always in groups of three).
      delimiter: " "
      # Number of decimals, behind the separator (the number 1 with a precision of 2 gives: 1.00).
      precision: 3

    # Used in number_to_currency().
    currency:
      format:
        # Where is the currency sign? %u is the currency unit, %n the number (default: $5.00).
        format: "%n %u"
        unit: "€"
        # These three are to override number.format and are optional.
        separator: ","
        delimiter: " "
        precision: 2

    # Used in number_to_percentage().
    percentage:
      format:
        # These three are to override number.format and are optional.
        # separator:
        delimiter: ""
        # precision:

    # Used in number_to_precision().
    precision:
      format:
        # These three are to override number.format and are optional.
        # separator:
        delimiter: ""
        # precision:

    # Used in number_to_human_size().
    human:
      format:
        # These three are to override number.format and are optional.
        # separator:
        delimiter: ""
        precision: 1
      storage_units:
        # Storage units output formatting.
        # %u is the storage unit, %n is the number (default: 2 MB).
        format: "%n %u"
        units:
          byte:
            one:   "tavu"
            other: "tavua"
          kb: "kt"
          mb: "Mt"
          gb: "Gt"
          tb: "Tt"

  # Used in distance_of_time_in_words(), distance_of_time_in_words_to_now(), time_ago_in_words().
  datetime:
    distance_in_words:
      half_a_minute: "puoli minuuttia sitten"
      less_than_x_seconds:
        one:   "alle sekunti sitten"
        other: "alle %{count} sekuntia sitten"
      x_seconds:
        one:   "1 sekunti sitten"
        other: "%{count} sekuntia sitten"
      less_than_x_minutes:
        one:   "alle minuutti sitten"
        other: "alle %{count} minuuttia sitten"
      x_minutes:
        one:   "1 minuutti sitten"
        other: "%{count} minuuttia sitten"
      about_x_hours:
        one:   "noin 1 tunti sitten"
        other: "noin %{count} tuntia sitten"
      x_days:
        one:   "1 päivä sitten"
        other: "%{count} päivää sitten"
      about_x_months:
        one:   "noin 1 kuukausi sitten"
        other: "noin %{count} kuukautta sitten"
      x_months:
        one:   "1 kuukausi sitten"
        other: "%{count} kuukautta sitten"
      about_x_years:
        one:   "noin 1 vuosi sitten"
        other: "noin %{count} vuotta sitten"
      over_x_years:
        one:   "yli 1 vuosi sitten"
        other: "yli %{count} vuotta sitten"
      almost_x_years:
        one:   "lähes vuosi sitten"
        other: "lähes %{count} vuotta sitten"
  models:
    errors:
      template:
        header:
          one:    "1 virhe estää tallentamasta kohteen %{model} muutoksia"
          other:  "%{count} virhe estää tallentamasta kohteen %{model} muutoksia"
        body: "Havaitsimme ongelmia seuraavissa kentissä:"
